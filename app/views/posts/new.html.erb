<div id='active_user'>
 <%= "You are signed in, #{current_user.name}." %> 
</div>
<br>
<%= render "welcome" %>

<div id='post_form'>
  <h1>New Post</h1>
  <%= render "form", article: @posts %>
</div>

<%= link_to "See recent posts", posts_url %> 